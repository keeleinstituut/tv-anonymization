apiVersion: v1
kind: Service
metadata:
  name: {{ include "tv-anonymise-api.fullname" . }}
  labels:
    {{- include "tv-anonymise-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tv-anonymise-api.selectorLabels" . | nindent 4 }}
