apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tv-anonymise-api.fullname" . }}-test-connection"
  labels:
    {{- include "tv-anonymise-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tv-anonymise-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
